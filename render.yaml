services:
  - type: web
    name: youtube-downloader
    repo: <REPO-URL-HERE>
    branch: main
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
